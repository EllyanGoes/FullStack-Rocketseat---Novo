/*
 A repetição acontece até que a repetição seja falsa 

 - Variável de controle 
 - Condição
 - Incremento ou decremento da variável de controle 



//iteração são a voltas, as repetições
for(step = 0; step < 10; step++){
  console.log(step)
}
*/

// Exemplo de tabuada 
let number = 7

for(step = 0; step <= 10; step++){
  console.log(`${number} x ${step} = ${number*step}`)
}
