// O método some() testa se ao menos um dos elementos no array passa na condição e reorna um valor true ou false

//Exemplo de um array de idade

const ages = [15, 30, 39, 29];

const result = ages.some((age) => age < 18);
