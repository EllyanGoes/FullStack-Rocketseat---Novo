// O método eery testa se todos os elementos do array passam na condição e retorna um valor Booelan.

//Exemplo de array de idades
const ages = [18, 30, 39, 29];

//Verificando se todas as idades sõ maiores ou igual a 18.

const result = ages.every((age) => age >= 18);

console.log(result);
